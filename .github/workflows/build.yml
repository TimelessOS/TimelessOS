name: Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run a multi-line script
        run: |
          cd $GITHUB_WORKSPACE
          ./build-server.sh
# Potentially release the image?
